import EarthScene from '../common/EarthScene';

const EarthSection = () => {
  return (
    <div className="h-screen w-full bg-black bg-center">
      <EarthScene />
    </div>
  );
};

export default EarthSection;
